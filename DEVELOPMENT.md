# 洛克王国 Wiki 插件开发文档

> 本文档记录项目开发过程中的关键发现、技术决策和注意事项。每次修复bug或新增功能前必须先阅读此文档。

---

## 📋 目录

1. [进化链数据结构](#进化链数据结构)
2. [查询意图识别规则](#查询意图识别规则)
3. [数据库字段说明](#数据库字段说明)
4. [常见问题与解决方案](#常见问题与解决方案)

---

## 进化链数据结构

### 数据来源

**重要发现**：BiliGame Wiki 的宠物页面 wikitext 中**没有** `{{进化链}}` 模板，进化链数据存储在 **Template:进化** 模板页面中。

- **模板页面**: `Template:进化`
- **获取方式**: 通过 `action=raw` 获取原始 wikitext
- **解析方法**: 从模板中提取所有进化链定义

### 数据存储位置

- **字段名**: `evolution_stages` (JSON格式)
- **存储表**: `pets`
- **关联字段**: `initial_stage_name` (初始形态名称，用于多分支进化关联)

### 数据结构示例

以"喵喵→喵呜→魔力猫"为例：

```json
[
  {
    "stage": 1,
    "name": "喵喵",
    "level": "16",
    "condition": ""
  },
  {
    "stage": 2,
    "name": "喵呜",
    "level": "32",
    "condition": ""
  },
  {
    "stage": 3,
    "name": "魔力猫",
    "level": "",
    "condition": ""
  }
]
```

**关键字段说明**：
- `stage`: 阶段序号（从1开始）
- `name`: 该阶段的宠物名称
- `level`: **达到该阶段所需的等级**（注意：存储在当前阶段，表示从上一阶段进化到当前阶段需要的等级）
  - 例如：喵呜的 `level: "16"` 表示从喵喵进化到喵呜需要16级
  - 例如：魔力猫的 `level: "32"` 表示从喵呜进化到魔力猫需要32级
- `condition`: **特殊进化条件**（如果有）

### ⚠️ 重要发现与验证结论

#### 1. 多分支进化通过 `initial_stage_name` 关联

**示例**：书魔虫有2个进化分支
- 分支1: 书魔虫 → 书卷守护 → 古卷执政官
- 分支2: 书魔虫 → 书卷守护 → 古卷匣魔像

**数据库表现**：
```python
# 古卷执政官
{
  "name": "古卷执政官",
  "initial_stage_name": "书魔虫",
  "evolution_stages": [
    {"stage": 1, "name": "书魔虫", "level": "16"},
    {"stage": 2, "name": "书卷守护", "level": "36"},
    {"stage": 3, "name": "古卷执政官", "level": ""}
  ]
}

# 古卷匣魔像
{
  "name": "古卷匣魔像",
  "initial_stage_name": "书魔虫",
  "evolution_stages": [
    {"stage": 1, "name": "书魔虫", "level": "16"},
    {"stage": 2, "name": "书卷守护", "level": "36"},
    {"stage": 3, "name": "古卷匣魔像", "level": ""}
  ]
}
```

**查询逻辑**：
- 查询"书魔虫的进化"时，通过 `initial_stage_name = '书魔虫'` 找到所有相关宠物
- 去重后展示所有不同的进化路线

#### 2. 进化条件存储规则

**核心规则**：进化条件存储在**当前阶段**的 `level` 和 `condition` 字段中，表示**从上一阶段进化到当前阶段**的条件。

**示例**：查询"喵喵怎么进化"
- 应该输出**喵呜**（下一阶段）的信息
- 喵呜的 `level: "16"` 表示从喵喵进化到喵呜需要16级
- 实现逻辑：找到当前宠物在进化链中的位置，输出下一个阶段的 `level` 和 `condition`

#### 3. Template:进化 模板格式

**模板结构**：
```
|喵喵|喵呜|魔力猫={{进化/个|喵喵|喵呜|魔力猫|进化1=16|进化2=32}}
|古卷匣魔像={{进化/个|书魔虫|书卷守护|古卷匣魔像|进化1=16|进化2=36}}
```

**解析要点**：
- 左侧：宠物名称列表（用 `|` 分隔），可能只有一个宠物（多分支的后续分支）
- 右侧：`{{进化/个|...}}` 模板调用
  - 第一个参数：完整的进化链宠物列表
  - 后续参数：`进化N=等级` 表示从第N阶段进化到第N+1阶段的条件

**特殊情况处理**：
- 当左侧只有一个宠物时（如 `|古卷匣魔像=`），需要从右侧模板中提取完整的进化链
- `initial_stage_name` 应使用右侧模板的第一个宠物名称

#### 4. 当前数据库状态

✅ **已完成**：
- 从 Template:进化 解析了 454 个宠物的进化链数据
- 更新了数据库中所有相关宠物的 `evolution_stages` 和 `initial_stage_name` 字段
- 支持多分支进化查询（如书魔虫的两个分支）

⚠️ **注意事项**：
- 部分宠物可能没有进化链（如迪莫、记忆石等单阶段宠物）
- 某些宠物的进化条件为空，表示只需要达到等级即可进化

### 爬虫解析逻辑（已废弃）

**原方法**：从宠物页面 wikitext 中解析 `{{进化链}}` 模板
**问题**：Wiki 宠物页面中没有此模板
**新方法**：从 `Template:进化` 页面批量解析所有进化链

**脚本位置**：`update_evolution_from_template.py`
**使用方法**：
```bash
python update_evolution_from_template.py wiki-local.db
```

---

## 查询意图识别规则

### 进化相关查询

文件：`main.py` - `_parse_type_query()` 方法

#### 支持的查询格式

| 查询示例 | 匹配规则 | 提取的宠物名 |
|---------|---------|------------|
| "迪莫的进化" | `^([^\s的]+?)进化$` | "迪莫" |
| "迪莫怎么进化" | `([^\s的]+?)(?:怎么进化\|...)` | "迪莫" |
| "灵狐的下一阶段" | `([^\s的]+?)(?:...\|下一阶段)` | "灵狐" |
| "墨鱿士的第二阶段" | `([^\s的]+?)(?:的第([一二三四五]\|[1-5])阶段)` | "墨鱿士" |
| "书魔虫的所有进化路线" | `([^\s的]+?)(?:完整进化链\|进化链\|进化路线\|全部进化)` | "书魔虫" |

#### ⚠️ 正则表达式注意事项

所有宠物名称提取的正则都必须排除"的"字，使用 `[^\s的]+?` 而不是 `\S+?` 或 `.+?`：

```python
# ✅ 正确
pet_evolution_query = re.search(r'([^\s的]+?)(?:怎么进化|如何进化|...)', query)

# ❌ 错误 - 会提取到"迪莫的"
pet_evolution_query = re.search(r'([\S]+?)(?:怎么进化|如何进化|...)', query)
```

### 已修复的正则表达式（共11处）

1. `pet_skills_query`: `([^\s的]+?)`
2. `pet_ability_query`: `([^\s的]+?)`
3. `pet_final_form_query`: `([^\s的]+?)`
4. `pet_evolution_query`: `([^\s的]+?)`
5. `simple_evolution`: `([^\s的]+?)` ← **关键修复**
6. `pet_after_evolution_query`: `([^\s的]+?)`
7. `pet_before_evolution_query`: `([^\s的]+?)`
8. `pet_full_evolution_query`: `([^\s的]+?)`
9. `pet_stage_query`: `([^\s的]+?)`
10. `pet_stats_query`: `([^\s的]+?)`

---

## 数据库字段说明

### pets 表关键字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `name` | TEXT | 宠物名称 |
| `initial_stage_name` | TEXT | 初始形态名称（用于关联多分支进化） |
| `evolution_stages` | TEXT (JSON) | 进化阶段数组（JSON格式） |
| `evolution_condition` | TEXT | 进化条件（已废弃，使用 evolution_stages 中的 condition） |
| `sprite_image_local` | TEXT | 本地图片路径 |

---

## 常见问题与解决方案

### Q1: 查询“XX怎么进化”返回“未找到宠物”

**原因**：
1. 宠物名称提取错误（包含“的”字）
2. 数据库中该宠物没有进化链数据

**解决方案**：
1. 检查正则表达式是否正确排除“的”字（使用`[^\s的]+?`）
2. 检查数据库中是否有该宠物的 `evolution_stages` 数据
3. 如果Wiki没有提供进化链数据，需要从 `Template:进化` 模板获取

**已修复的正则位置**：
- `main.py` 第2296行：`pet_evolution_query`
- `main.py` 第2331行：`pet_full_evolution`
- 所有其他宠物名称提取处（共11处）

### Q2: 查询“XX的进化路线”或“XX的所有进化路线”返回错误

**现象**：
- “灵狐的进化路线” → “未找到与 '灵狐的进化路线' 相关的信息”
- “墨鱿士的所有进化路线” → “未找到宠物 '所有'”

**原因**：正则表达式使用`.+?`匹配到了“的”字，导致提取的宠物名包含“的”

**解决方案**：
- 已将 `pet_full_evolution` 的正则改为 `[^\s的]+?`
- 位置：`main.py` 第2331行

### Q3: 查询“XX的进化”返回“暂无进化信息”

**现象**：“喵喵的进化” → “(暂无进化信息)”

**原因**：缺少对“XX的进化”这种简单格式的匹配规则

**解决方案**：
- 新增 `simple_pet_evolution` 规则，匹配 `^([^\s的]+?)的进化$`
- 位置：`main.py` 第2338-2344行
- 该规则会将查询类型设为 `pet_evolution_query`，显示下一阶段信息

### Q4: 多分支进化如何查询

**示例**：书魔虫有2个进化分支

**查询方式**：
- "书魔虫的进化" - 显示所有分支
- "书魔虫的最终形态" - 显示所有最终形态（古卷执政官、古卷匣魔像）

**实现逻辑**：
```python
# 通过 initial_stage_name 查询所有相关宠物
all_chains = db_service.get_pet_all_evolution_chains(pet_name)
```

### Q3: 进化条件在哪里

**答案**：进化条件存储在**下一阶段**的 `level` 和 `condition` 字段中。

**示例**：
- 查询"喵喵怎么进化"
- 应该输出喵呜（下一阶段）的信息：
  - `level: "16"` - 需要16级
  - `condition: ""` - 无特殊条件

---

## 开发流程规范

### 必须遵守的开发流程

1. **在本地修改代码**
2. **在本地测试完毕**
3. **同步至本地Linux服务器** (`scp`)
4. **远程测试读取AstrBot日志** (`ssh linux "tail -f ..."`)
5. **出现问题，再次返回步骤1，直至无错误**
6. **由用户手动测试并反馈**

### ⚠️ 禁止的操作

- ❌ 不要直接在服务器上修改代码
- ❌ 不要在未本地测试的情况下同步到服务器
- ❌ 不要跳过任何步骤

---

## 更新日志

### 2026-04-10 - 进化查询正则全面优化

**问题发现**（用户测试反馈）：
1. "喵喵的进化"显示等级位置错误
2. "灵狐的下一阶段"返回完整进化链而非下一阶段
3. 多种口语化表达无法识别（如"会进化成什么"、"第二阶段"无"的"字等）

**根本原因**：
1. level字段存储逻辑理解错误：level存储在**当前阶段**，表示从当前阶段进化到下一阶段需要的等级
2. 缺少独立的"下一阶段"查询类型
3. 正则表达式过于严格，不支持无"的"字的口语化表达

**解决方案**：

#### A. 修复进化链显示逻辑
- **第2628-2641行**：单条进化链显示，改为从`stage`而非`next_stage`获取level
- **第2588-2602行**：多分支共同路径，同样修复
- **第2605-2622行**：多分支分岔路径，更新注释说明

#### B. 创建独立的下一阶段查询类型
- **第2303-2309行**：新增`pet_next_stage_query`类型
  ```python
  pet_next_stage = re.search(r'([^\s]+?)(?:的)?(?:下一阶段|下一个是什么|后面是什么|下一步)', query)
  ```
- **第2650-2717行**：添加处理逻辑，只显示下一个进化形态+完整路线

#### C. 扩展正则表达式支持更多表达
1. **最终形态查询**（第2288行）
   - 修复前：`r'([^\s的]+?)(?:的最终形态|...)`
   - 修复后：`r'([^\s]+?)(?:的)?(?:最终形态|...)`
   - 支持："灵狐最终形态"（无"的"）

2. **进化查询**（第2296行）
   - 新增关键词：`会进化成|会变成|变成什么|进化成啥`
   - 支持："喵喵会进化成什么"、"喵喵变成什么"

3. **下一阶段查询**（第2304行）
   - 新增关键词：`下一个是什么|后面是什么|下一步`
   - 支持："灵狐下一个是什么"、"灵狐后面是什么"

4. **特定阶段查询**（第2361行）
   - 修复前：`r'([^\s的]+?)(?:的第([一二三四五]|[1-5])阶段|...)`
   - 修复后：`r'([^\s]+?)(?:的)?(?:第([一二三四五]|[1-5])阶段|第([一二三四五]|[1-5])阶|([一二三四五]|[1-5])阶)`
   - 支持："墨鱿士第二阶段"（无"的"）、"墨鱿士二阶"（无"第"）
   - 更新group索引：支持3个捕获组（group 2/3/4）

**验证结果**：
- ✅ "喵喵的进化" → 正确显示等级在对应阶段下方
- ✅ "灵狐的下一阶段" → 显示九尾狐及完整路线
- ✅ "喵喵会进化成什么" → 正确识别
- ✅ "墨鱿士第二阶段" → 正确识别（无"的"）
- ✅ "墨鱿士二阶" → 正确识别（无"第"）

**关键知识点**：
- `([^\s]+?)(?:的)?` 模式：允许"的"作为可选分隔符，同时排除空格
- level字段存储规则：**存储在当前阶段**，表示进化到下一阶段的条件
- 查询类型分离："下一阶段"应独立于"进化查询"，语义不同

**文件变更**：
- 修改：`main.py` - 修复3处显示逻辑，新增1种查询类型，扩展4类正则表达式

**问题发现**（用户测试反馈）：
1. "灵狐的进化路线" → 返回"未找到与 '灵狐的进化路线' 相关的信息"
2. "墨鱿士的所有进化路线" → 返回"未找到宠物 '所有'"
3. "喵喵的进化" → 返回"暂无进化信息"

**根本原因**：
- 正则表达式使用`.+?`或`\S+?`会匹配到"的"字，导致宠物名称提取错误
- 缺少"XX的进化"这种简单格式的匹配规则

**解决方案**：
1. **修复所有进化相关正则**：将`.+?`和`\S+?`改为`[^\s的]+?`
   - 第2296行：`pet_evolution_query` - 详细进化查询
   - 第2331行：`pet_full_evolution` - 完整进化链查询
   
2. **新增简单进化查询规则**：
   ```python
   # "某精灵的进化"（简单进化查询）
   simple_pet_evolution = re.search(r'^([^\s的]+?)的进化$', query)
   if simple_pet_evolution:
       return {
           'type': 'pet_evolution_query',
           'pet_name': simple_pet_evolution.group(1).strip()
       }
   ```

**验证结果**：
- ✅ "灵狐的进化路线" → 正确提取"灵狐"，显示进化链
- ✅ "墨鱿士的所有进化路线" → 正确提取"墨鱿士"，显示多分支进化
- ✅ "喵喵的进化" → 正确匹配，显示下一阶段（喵呜）及进化条件

**关键知识点**：
- `[^\s的]+?` 表示匹配一个或多个非空格、非"的"字的字符（非贪婪）
- 这样可以避免"迪莫的进化"被提取为"迪莫的"
- 所有涉及宠物名称提取的正则都应遵循此规范

**文件变更**：
- 修改：`main.py` - 修复2处正则表达式，新增1条匹配规则

### 2026-04-10 - 进化链数据修复

**问题发现**：
- 数据库中所有宠物的 `evolution_stages` 为空（0个阶段）
- 原因：Wiki宠物页面中没有 `{{进化链}}` 模板

**解决方案**：
- 从 `Template:进化` 模板页面获取进化链数据
- 创建 `update_evolution_from_template.py` 脚本解析模板
- 成功解析并更新 454 个宠物的进化链数据

**关键技术点**：
1. **多分支进化处理**：书魔虫有两个进化分支，通过 `initial_stage_name` 关联
2. **初始形态提取**：当左侧只有一个宠物时，从右侧模板提取第一个宠物作为初始形态
3. **进化条件存储**：`level` 字段存储在当前阶段，表示从上一阶段进化到当前阶段的等级要求
4. **正则表达式修复**：修正了宠物名称提取逻辑，排除“的”字干扰（11处）
5. **关键词扩展**：添加“下一阶段”关键词匹配

**验证结果**：
- ✅ 喵喵进化链：喵喵(16级) → 喵呜(32级) → 魔力猫
- ✅ 书魔虫双分支：
  - 分支1: 书魔虫 → 书卷守护 → 古卷执政官
  - 分支2: 书魔虫 → 书卷守护 → 古卷匣魔像
- ✅ 所有宠物的 `initial_stage_name` 正确设置

**文件变更**：
- 新增：`update_evolution_from_template.py` - 进化链数据更新脚本
- 新增：`fetch_evolution_template.py` - Template:进化获取脚本
- 修改：`main.py` - 修复正则表达式，添加“下一阶段”关键词
- 修改：`DEVELOPMENT.md` - 记录进化链数据结构和技术细节

### 2026-04-10 - 早期修复

- ✅ 修复宠物名称提取正则，排除“的”字（11处）
- ✅ 添加“下一阶段”关键词匹配
- ⚠️ 发现数据库中大多数宠物的 `evolution_stages` 为空

---

**最后更新**: 2026-04-10  
**维护者**: InMain
