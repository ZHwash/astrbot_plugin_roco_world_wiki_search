# 仓库分支使用说明

## 📦 分支说明

为了解决 AstrBot 插件包体积限制（< 16MB）的问题，本项目采用双分支策略：

### 1. 主分支（main）
- **用途**：AstrBot 插件市场自动部署
- **包含内容**：代码、配置文件
- **体积**：约 7MB ✅
- **适用场景**：服务器部署、插件市场安装

### 2. 图片分支（images-branch）
- **用途**：本地开发和使用
- **包含内容**：代码 + 数据库（wiki-local.db）+ 所有图片资源
- **体积**：约 320MB
- **适用场景**：本地测试、离线使用

---

## 🚀 使用方法

### 方法一：使用主分支（推荐服务器部署）

1. **克隆主分支**
   ```bash
   git clone https://github.com/ZHwash/astrbot_plugin_roco_world_wiki_search.git
   cd astrbot_plugin_roco_world_wiki_search
   ```

2. **通过管理员命令获取数据**
   
   在 AstrBot 中发送：
   ```
   /洛克管理 update
   ```
   
   这会自动从 Wiki 爬取最新数据并生成数据库和图片。

3. **优点**
   - ✅ 数据最新
   - ✅ 无需手动管理大文件
   - ✅ 适合自动化部署

---

### 方法二：使用图片分支（推荐本地使用）

1. **克隆主分支**
   ```bash
   git clone https://github.com/ZHwash/astrbot_plugin_roco_world_wiki_search.git
   cd astrbot_plugin_roco_world_wiki_search
   ```

2. **切换到图片分支**
   ```bash
   git checkout images-branch
   ```

3. **直接使用**
   - 数据库和图片已包含在仓库中
   - 无需额外下载或爬取
   - 可以离线使用

4. **优点**
   - ✅ 开箱即用
   - ✅ 无需等待爬取
   - ✅ 适合本地开发和测试

---

## 🔄 分支切换

### 从主分支切换到图片分支
```bash
git checkout images-branch
```

### 从图片分支切换回主分支
```bash
git checkout main
```

---

## ⚠️ 注意事项

1. **不要将图片分支推送到 AstrBot 插件市场**
   - 图片分支体积过大（320MB），超过 AstrBot 的 16MB 限制
   - 只使用主分支进行插件市场发布

2. **定期同步更新**
   ```bash
   # 在主分支上
   git pull origin main
   
   # 在图片分支上
   git pull origin images-branch
   ```

3. **图片分支的更新**
   - 图片分支会定期合并主分支的代码更新
   - 如需最新图片，建议重新运行 `/洛克管理 update`

---

## 💡 常见问题

### Q: 我应该使用哪个分支？
**A:** 
- 如果是服务器部署 → 使用主分支 + `/洛克管理 update`
- 如果是本地开发 → 使用图片分支

### Q: 为什么不能把所有文件放在一个分支？
**A:** 
- AstrBot 插件市场对包体大小有限制（< 16MB）
- 图片资源占用约 319MB，远超限制
- 采用双分支策略可以同时满足两种使用场景

### Q: 图片分支的数据会过时吗？
**A:** 
- 是的，图片分支的数据是快照
- 建议定期切换到主分支运行 `/洛克管理 update` 获取最新数据
- 或者等待图片分支的定期更新

### Q: 如何贡献代码？
**A:** 
- 请在主分支（main）上进行开发和提交 PR
- 图片分支仅用于存储数据，不接受代码修改

---

## 📊 仓库统计

| 分支          | 文件大小 | Git 跟踪文件数 | 主要用途 |
|-------------|---------|--------------|---------|
| main        | ~7MB | 35 | 插件市场部署 |
| images-branch | ~320MB | 2773 | 本地开发使用 |

---

**最后更新**: 2026-05-09
